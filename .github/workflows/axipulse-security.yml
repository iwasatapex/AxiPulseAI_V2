name: Security Scan

on:

  pull_request:

  push:

    branches:
      - master


jobs:

  security:

    runs-on: ubuntu-latest


    steps:

      - uses: actions/checkout@v4


      - name: Dependency audit
        run: |
          pip install pip-audit
          pip-audit || true
